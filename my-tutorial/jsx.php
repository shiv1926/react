<?php include('header.php'); ?>
<ul>
	<li>JSX has two names. Javascript XML and Javascript Syntax Extension.</li>
	<li>In XML we can create custom tags like that we can create custom tags in react also.</li>
	<li>JSX is a syntax extension, instead of writing actual react elements we use JSX syntax. becuse it is much simpler. </li>
	<li>JSX produce react elements. It product React.CreateElement tag.</li>
	<li>JSX element looks like html tag.</li>
	<li>
		We should always use camel case in react
		<ul>
			<li>Upper camel case : for file naming, for component naming</li>
			<li>lower camel case : for variable namig, for function naming</li>
		</ul>
	</li>
</ul>
<?php include('footer.php'); ?>